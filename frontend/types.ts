// TODO: Delete marked items when finished using fake product data
export interface Product {
  id: string;
  title: string;  // delete
  // name: string;
  price: number;  // delete
  // priceInCents: number;
}

export interface Account {
  id: number;
  name: string;
  email: string;
}
